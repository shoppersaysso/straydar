"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = mapKeys;
/**
 * Create a new object with keys returned by iterator.
 * @param {Object} object
 * @param {function} iterator function(value, key) returning key
 * @returns {Object} a new object with same values as object and mapped keys
 */
function mapKeys(object, iterator) {
  var result = {};
  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = Object.keys(object)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var key = _step.value;

      var value = object[key];
      result[iterator(value, key)] = value;
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator.return) {
        _iterator.return();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  return result;
}